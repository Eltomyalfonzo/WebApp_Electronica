version: "3"
services:
  mosquitto:
    image: eclipse-mosquitto
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - ./mosquitto.conf:/mosquitto/config/mosquitto.conf

  webapp:
    build: ./webapp
    ports:
      - "3000:3000"
    depends_on:
      - mosquitto
